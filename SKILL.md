---
name: logistics-report-generator
description: 物流周报/月报自动生成器。当用户提供寄件运单Excel明细（含运单号、寄件/签收时间、目的省份、件数、重量、备注等），需要为物流客户生成可直接发送的周度或月度物流报表（含与上期对比的改善数据、时效达成、异常件处理、改善方案与服务承诺）时使用。适用于快递/物流网点经营者为项目客户定期做报表的场景，触发词如"周报""物流周报""月报""生成报表""数据对比"等。支持一次性生成本期对比上期（上周/上月）的两页A4 PDF报表。
---

# 物流周报/月报生成器

## Overview

读取客户一周/一月的寄件运单 Excel 明细，自动完成全量分析（总量、每日/每周、省份、重量段、签收时效、异常件分类），并与上一期对比，输出两页 A4 PDF 物流报表。报表含：KPI 卡片、周（月）度数据改善对比、时效达成情况、异常件处理、时效达成明细、改善方案与服务承诺。可直接发送给客户。

## 快速开始（周报，含与上周对比）

```bash
python3 scripts/build_report.py \
  --current "本周明细.xlsx" \
  --previous "上周明细.xlsx" \
  --client "卡乐" \
  --output "./out"
```

输出 `./out/<客户>客户_物流周报.html` 与 `.pdf`，脚本打印 PDF 绝对路径与页数。

## 使用场景与命令

### 1. 周报（每周一次，本周 vs 上周）
```bash
python3 scripts/build_report.py --current 本周.xlsx --previous 上周.xlsx --client 卡乐 --output ./out
```
- `--current`：本周明细 Excel（可传多个，逗号分隔，将自动合并）
- `--previous`：上周明细 Excel，用于环比；**不传则输出"本期核心数据概览"版**（第一期适用）

### 2. 月报（每月一次，本月 vs 上月）
```bash
python3 scripts/build_report.py --mode monthly \
  --current "8月第1周.xlsx,8月第2周.xlsx,8月第3周.xlsx,8月第4周.xlsx" \
  --previous "7月第1周.xlsx,7月第2周.xlsx,..." \
  --client 卡乐 --output ./out
```
- 也可传一个**目录**（`--current ./8月/`），脚本自动读取目录下所有 xlsx
- 月报按"月内周"聚合展示（第1周=1-7日…第5周=29日及以后），对比上月
- 周报、月报共用同一脚本与模板，仅标题、周期、聚合粒度、对比标签不同

### 3. 常用参数
| 参数 | 说明 |
|---|---|
| `--client` | 客户名称，用于标题与页脚（默认"客户"） |
| `--period-label` | 手动指定周期标签（如"2026年8月24日—8月29日"）；缺省自动从数据日期推断 |
| `--report-date` | 报告出具日期，默认今天 |
| `--skip-pdf` | 只生成 HTML，不转 PDF |
| `--output` | 输出目录 |

## 输入数据格式（自动兼容列名差异）

支持常见列名（自动识别），缺列自动降级、不报错：

| 标准列 | 兼容别名 |
|---|---|
| 运单号 | 运单编号 / 单号 / 订单号 |
| 寄件时间 | 发货时间 / 下单时间 / 寄件日期 |
| 签收时间 | 收货时间 |
| 目的省份 | 省份 / 目的省 / 收件省份 |
| 件数 | 总件数 |
| 实际重量 | 重量 / 实际重 |
| 结算重量 | 计费重量 / 结算重 / 计费重 |
| 体积 | 体积重量 / 体积重 |
| 备注 | Unnamed: 10 / 说明 / note（含"备注"字样均可） |

- 备注列是**异常件分类**的唯一来源，请保证能导出备注/说明列
- 一票多件、重量、体积、时间列类型可自动解析

## 输出报表结构（两页 A4）

- **第1页**：头部（客户+周期）、4 个 KPI 卡片、① 周(月)度数据改善对比（4 个高亮改善卡 + 10 项指标对比表）、② 时效达成情况（分级进度条 + 签收时效分布）、③ 异常件处理情况（3 个状态卡 + 异常类型明细表）
- **第2页**：④ 时效达成明细（每日/每周时效 + 主要省份签收）、⑤ 异常件处理情况续（异常分布 + 重点异常件说明）、⑥ 改善方案与服务承诺、概况注

改善方案与重点异常说明**由脚本根据数据自动生成**（如存在"未赶上清仓"则生成清仓衔接优化；存在签收率<85%的省份则生成偏远专项跟踪；存在缺单证则生成单证核对前置等），无需手工编写。

## 环境要求与经验

- 依赖：`pandas`、`numpy`、`openpyxl`（`pip3 install pandas numpy openpyxl`）；PDF 转换依赖 Chrome
- Chrome 路径自动探测（macOS 默认 `/Applications/Google Chrome.app/...`，也支持 Edge/Chromium/命令行 chrome）
- 生成 PDF 后脚本会用二进制正则校验页数；目标 2 页，若实际≠2 会打印警告，需人工检查排版是否溢出
- **时效口径**：若寄件时间为日期粒度（无时分），脚本自动在报告对比处加注"平均时效略高于实际"，避免与精确时分数据直接对比造成误解
- 异常分类按备注关键词规则（分拨/清仓/单证/预约/周末休息/顺延/网点等），同一备注取第一个命中项
- **在途不计异常**：备注为"派送途中/派件途中/转运途中/正常中转中/已到达派件站点/已交接派件站点"等属正常在途流转，脚本自动不计入异常件，并在报告注释说明
- **期末截点效应**：统计截止日前1天发货集中时（如周日发货），部分快件仍在途未签收，会拉低整体/省份签收率。脚本自动把截止日前1天内发出的未签收件视为在途截点：省份签收评估排除截点后再评级，避免误标"重点线路"、误生成偏远专项改善方案
- **缺件数列**：源文件无"件数/体积"列时，总件数自动按运单数计（每单1件）并在 KPI 卡片与对比表加注，避免与真实件数混淆

## 交付建议

- 生成后检查：① PDF 为 2 页；② 对比表"上周/本月"列有数据（第一期除外）；③ 异常件数量与备注条数一致
- 交付方式：直接向用户提供 PDF 绝对路径/用交付工具展示 PDF；同时保留 HTML 便于后续微调
- 若客户名含特殊字符，输出文件名会自动清洗
